use imported::sums::*;

fn add_i64(n: i64, m: i64) -> i64 {
    add(n, m)
}

fn add_str(n: str, m: str) -> str {
    concat_s(n, m)
}

fn add_str_char(n: str, m: char) -> str {
    concat_c(n, m)
}


struct Time {
    HID: i64
}
let hours_in_day = 24;
let time = Time { HID: hours_in_day};
